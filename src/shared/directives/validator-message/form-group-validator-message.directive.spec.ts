import { FormGroupValidatorMessageDirective } from './form-group-validator-message.directive';

describe('FormGroupValidatorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupValidatorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
